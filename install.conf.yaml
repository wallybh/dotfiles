- defaults:
    link:
        relink: true
- clean: ['~']
- link:
    ~/.gitconfig:
        path: git/gitconfig
    ~/.gitconfig.aliases:
        path: git/gitconfig.aliases
    ~/.zsh:
    ~/.zshrc:
- shell:
    - [git submodule update --init --recursive, Installing submodules]
